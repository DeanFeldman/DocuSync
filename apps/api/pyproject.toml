[project]
name = "documentsync-api"
version = "1.0.0"
description = "DocumentSync desktop DOCX API"
requires-python = ">=3.11"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
